<?xml version="1.0" encoding="utf-8"?>
<odoo>

   <record id="library.rental_tree" model="ir.ui.view">
        <field name="name">library.rental.tree</field>
        <field name="model">library.rental</field>
        <field name="arch" type="xml">
            <tree>
                <field name="book_copy_id"/>
                <field name="customer_id"/>
                <field name="rental_date"/>
                <field name="return_date"/>
            </tree>
        </field>
    </record>

   <record id="library.rental_form" model="ir.ui.view">
        <field name="name">library.rental.form</field>
        <field name="model">library.rental</field>
        <field name="arch" type="xml">
            <form>
                <sheet>
                    <group>
                        <field name="customer_id"/>
                        <field name="book_copy_id"/>
                        <field name="total_owed" string="Total owed for this customer"/>
                    </group>
                    <group>
                        <field name="rental_date"/>
                        <field name="return_date"/>
                    </group>
                    <group>
                        <field name="is_lost"/>
                        <field name="amount_owed"/>
                        <field name="paid"/>
                    </group>
                </sheet>
            </form>
        </field>
    </record>


    <record id="rental_action" model="ir.actions.act_window">
        <field name="name">Rentals</field>
        <field name="res_model">library.rental</field>
        <field name="view_mode">tree,form</field>
    </record>
</odoo>